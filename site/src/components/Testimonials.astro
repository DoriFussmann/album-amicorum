---
/**
 * TEMPORARY PLACEHOLDER TESTIMONIALS
 * Replace with authentic customer stories before launch.
 * Source: Website Specification v1.0 — Testimonials placeholders.
 */
const quotes = [
  {
    quote:
      'My daughter carried her Friend Book everywhere that summer. Looking through it now feels like opening a little time capsule.',
    author: 'Parent',
    tone: 'bg-blush/40',
  },
  {
    quote:
      "The illustrations are beautiful, but it's the handwriting that makes it so special. Every page feels like a memory.",
    author: 'Parent',
    tone: 'bg-oatmeal/70',
  },
  {
    quote:
      'It has become one of the books we reach for most often—not to read, but to remember.',
    author: 'Parent',
    tone: 'bg-primary-soft/15',
  },
];
---

<section id="testimonials" aria-labelledby="testimonials-heading" class="section-space">
  <div class="site-container">
    <div class="mx-auto mb-14 max-w-2xl text-center">
      <h2 id="testimonials-heading">A keepsake for years to come.</h2>
    </div>

    <!-- TEMPORARY: placeholder testimonials — replace before launch -->
    <ul class="grid gap-8 md:grid-cols-3" data-testimonials="placeholder">
      {
        quotes.map((item) => (
          <li>
            <blockquote
              class:list={[
                'flex h-full flex-col justify-between rounded-soft px-7 py-8',
                item.tone,
              ]}
            >
              <p class="font-display text-xl leading-[1.4] text-ink-heading md:text-2xl">
                “{item.quote}”
              </p>
              <footer class="mt-8 text-sm font-semibold tracking-wide text-ink">
                — {item.author}
              </footer>
            </blockquote>
          </li>
        ))
      }
    </ul>
  </div>
</section>
